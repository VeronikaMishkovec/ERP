// COLORS
export enum COLORS {
  CAPER = '#ABBA88',
  SWAMP = '#102020',
  WHITE = '#FFFFFF',
}

// FONT SIZE
export enum FONT_SIZE {
  BUTTON = 18,
  HEADER = 32,
  LABEL = 16,
}

// SCREENS HEADERS
export enum HEADERS {
  LOGIN = 'Log in',
}

// BUTTON & INPUTS LABELS
export enum LABELS {
  EMAIL = 'Email',
  LOGIN = 'Log in',
  PASSWORD = 'Password',
}

// PADDINGS
export enum PADDINGS {
  INPUT = 10,
  ROOT = 15,
}

// BORDER RADIUS
export enum RADIUS {
  BORDERS = 5,
}

// MARGINS
export enum MARGINS {
  BUTTON = 50,
}

// SIZES
export enum SIZES {
  BUTTON_HEIGHT = 50,
  BUTTON_WIDTH = 160,
}
