// COLORS
export enum COLORS {
  CHINA_IVORY = '#FCF3CA',
  TOLOPEA = '#0e0639',
  WHITE = '#FFFFFF',
}

// FONT SIZE
export enum FONT_SIZE {
  BUTTON = 18,
  DAY = 40,
  HEADER = 32,
  LABEL = 16,
  MONTH = 20,
  SUBHEADER = 26,
  WEEKDAY = 18,
}

// SCREENS NAMES
export enum SCREENS_NAMES {
  HOME = 'Home',
  LOGIN = 'Login',
}

// SCREENS HEADERS
export enum HEADERS {
  LOGIN = 'Log in',
}

// BUTTON & INPUTS LABELS
export enum LABELS {
  EMAIL = 'Email',
  LOGIN = 'Log in',
  PASSWORD = 'Password',
}

// PADDINGS
export enum PADDINGS {
  INPUT = 10,
  ROOT = 15,
}

// BORDER RADIUS
export enum RADIUS {
  BORDERS = 5,
}

// MARGINS
export enum MARGINS {
  BUTTON = 50,
  TIMER_CONTROLS = 20,
}

// SIZES
export enum SIZES {
  BUTTON_HEIGHT = 50,
  BUTTON_WIDTH = 160,
}
